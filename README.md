# 🤖 Telegram Bot для интерактивного опроса

Бот для проведения интерактивных опросов с мгновенной статистикой ответов.

## 🚀 Быстрый старт на Replit

1. **Fork этот репозиторий** на Replit
2. **Добавьте BOT_TOKEN** в Secrets (ключ: `BOT_TOKEN`)
3. **Нажмите Run** - бот запустится автоматически!

## 🛠 Локальная установка

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/telegram-poll-bot.git
cd telegram-poll-bot

# Установите зависимости
pip install -r requirements.txt

# Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env файл, добавив ваш BOT_TOKEN

# Запустите бота
python main.py
```

## ⚙️ Конфигурация

### Переменные окружения

- `BOT_TOKEN` - токен вашего Telegram бота (обязательно)

### Настройка вопросов

Вопросы настраиваются в файле `main.py` в переменной `QUESTIONS`:

```python
QUESTIONS = [
    "Ваш вопрос 1?",
    "Ваш вопрос 2?",
    # ... добавьте свои вопросы
]
```

## 📊 Функциональность

- ✅ Интерактивные кнопки "Да/Нет"
- 📈 Мгновенная статистика ответов
- 📊 Проценты и общее количество голосов
- 🌐 Веб-интерфейс для мониторинга
- 💾 Хранение результатов в памяти

## 🎯 Использование

1. Найдите бота в Telegram
2. Отправьте команду `/start`
3. Отвечайте на вопросы с помощью кнопок
4. Наблюдайте за обновляемой статистикой

## 📁 Структура проекта

```
telegram-poll-bot/
├── main.py              # Основной код бота
├── requirements.txt     # Зависимости Python
├── .replit             # Конфигурация Replit
├── .env.example        # Шаблон переменных окружения
└── README.md          # Документация
```

## 🔧 Технические детали

- **Python 3.8+**
- **python-telegram-bot 20.x**
- **Flask 2.x** (для веб-интерфейса)
- **Хранение в памяти** (результаты сбрасываются при перезапуске)

## 🚀 Деплой

### Replit
- Автоматический деплой из репозитория
- Бесплатный хостинг
- Простое управление переменными окружения

### Другие платформы
- Heroku
- Railway
- PythonAnywhere
- VPS с systemd
